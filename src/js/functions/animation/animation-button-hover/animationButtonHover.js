import Cursor from './cursor';
import ButtonCtrl from './buttonCtrl.js';

export const animationButtonHover = () => {
  // Анимация кнопок отключена
  /*
  // initialize custom cursor
  const cursor = new Cursor(document.querySelector('.cursor'));
  const button = new ButtonCtrl(document.querySelector('.btn-common'));

  button.on('enter', () => cursor.enter());
  button.on('leave', () => cursor.leave());
  */
}
